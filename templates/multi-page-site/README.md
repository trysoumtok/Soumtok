# {{title}}

Multi-page site (Home, About, Menu, Contact) with shared header/footer, scroll reveal, and design tokens.

```bash
npm install
npm run dev
```

Open http://localhost:5173 — navigate between pages via the header.
